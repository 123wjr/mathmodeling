# A题项目总控台

> 本页是全队唯一状态页。任务书定义“怎么做”，本页只回答“现在到哪、谁负责、什么算完成、下一步是什么”。每次交接后由总负责人更新本页，禁止用聊天中的“已完成”替代验收证据。

论文主笔 Agent 的仓库入口：[PAPER_AGENT_START_HERE.md](../PAPER_AGENT_START_HERE.md)。论文内容 canonical 源为 [论文草稿.md](../论文草稿.md)；在线文档是协作/提交副本。GitHub 同时保存论文源、代码、配置、结果和技术证据。

## 0. 当前快照

| 项目 | 当前事实 |
|---|---|
| 更新时间 | 2026-08-11 23:52 CST |
| 官方截止 | 2026-08-13 20:00 CST |
| 首次上传 | 2026-08-13 18:30 CST |
| 剩余时间 | 约 44 小时 08 分 |
| Git 基线 | `main @ origin/main commit`；G1-G4 实现、产物和证据包已同步 |
| 总体灯号 | **AMBER：G1-G4 技术证据已提交；等待论文数字/图冻结与红队复核** |
| 当前唯一目标 | 完成 A5 技术过渡包交接，并进入 G5 数字/图冻结；论文正文逐项回指事实表与证据包 |
| 当前总负责人决定 | 继续执行已签署的 **NMC + CALCE INR18650-20R 参考边界** 主线；CALCE 原始循环未拟合，NASA 未进入当前实验/验证 |

### 完成定义

- `GREEN`：产物存在、运行/核验记录存在、负责人验收通过。
- `AMBER`：正在做，责任人、截止和验收条件都明确。
- `RED`：存在冲突、缺关键依赖或已错过闸门；必须先解阻。
- `GRAY`：尚未开始，不把文件存在或 Agent 声称当作进度。

## 1. 唯一事实源与停止规则

发生冲突时按以下优先级裁决：

1. 总负责人签署的闸门结论与 `.codex/DECISIONS.md`。
2. 已验收且带版本/commit 的 A0-A5 证据包。
3. 可复跑的代码、测试、结果和哈希。
4. 任务书、在线文档草稿和 Agent 汇报。
5. 聊天中的数字、结论或“已经完成”。

**停止规则**：上一级事实与下一级文件冲突时，下一级不得继续生产结果。修复必须是新 commit，不重写历史，不静默删除队友材料。

当前有效技术边界：

| 项目 | 冻结值 |
|---|---|
| 主化学体系 | NMC（LiNiMnCo/graphite） |
| 主参考对象 | CALCE INR18650-20R；仅用于 NMC 对象与容量/内阻尺度参考，未读取原始循环拟合 |
| NASA 数据 | 未进入当前实验、训练、验证或参数估计；仅保留为未来独立外部对照候选 |
| 生成器 | 分段平方根容量衰减 + 内阻增长；固定工况修正、截断正态个体差异、测量噪声 |
| 工况边界 | 25--50 degC、0.5--2C、DOD 50/80/100、CC-CV |
| 自设参数 | 全部标记 `ASSUMED`；`knee_gain=2.0`；正式全因子 `seed=20260811`，G1 冒烟/部分 Q4 使用 `42` |
| 数据划分 | 同一 `cell_id` 的循环记录不得跨训练/测试组 |
| 写作边界 | 仿真不称实测；80% SOH 不称普适安全阈值；无证据数字不得入文 |
| 台账格式 | 本次维持已验收的 `.txt` 台账（内容为逗号分隔结构）；格式迁移推迟到赛后 |

## 2. G0-R 收口记录

| 证据 | 当前内容 | 判定 |
|---|---|---|
| `handoffs/G0_OWNER_REVIEW.md` | `PASS_WITH_CHANGES`；冻结 NMC + CALCE；允许进入 G1 | 已签署决定 |
| `.codex/DECISIONS.md` | 同样冻结 NMC + CALCE 与分段平方根生成器 | 已签署决定 |
| 远端 `main @ G0-R clean line` | NMC + CALCE；TXT 台账；G1 材料索引；无伪造 A1 | **G0-R PASS** |
| 历史 `53ab429` | LFP + Severson；`PENDING_APPROVAL`；空 A1 | 已从 `main` 移除，仅作审计历史 |
| `handoffs/G0_OWNER_REVIEW.md` | `PASS_WITH_CHANGES`；冻结 NMC + CALCE；允许进入 G1 | 已签署决定 |
| 当前本地工作区 | `main @ origin/main commit`；G1-G4 证据与技术文档已提交；A5 技术过渡包已在工作区待固化 | 可进入 G5 论文整合 |
| `code/g1_generator/`、`code/tests/`、`configs/`、`g1_output/` | 均存在；41 G1 tests passed；12 电芯、12000 行、4 个有效 SVG | G1 技术通过 |

远端已用受保护的 `force-with-lease` 更新；`data/` 内 NASA `.mat` 文件仍只作 `SECONDARY_ONLY`，不属于 G1 结果。队友开始论文整合前必须拉取远端 `main` 的当前 commit。

**结论**：科学决策 `G0 = PASS_WITH_CHANGES`；仓库同步 `G0-R = PASS`；实现 `G1 = PASS_WITH_CHANGES`。G2-G4 已完成可复跑验证并随证据包同步；下一阶段为论文整合与独立红队，不再新增模型范围。

## 3. 闸门总览

| 闸门 | 恢复排期 | Owner | 状态 | 通过证据 | 下一动作 |
|---|---:|---|---|---|---|
| G0-R 事实源对齐 | 8/10 17:44 | 建模负责人；总负责人验收 | **GREEN：PASS** | G0-R clean line；NMC/CALCE；TXT 台账；远端可见 | 已关闭 |
| G1 A1 仿真包 | 8/11 15:00 | 建模负责人；总负责人复核 | **GREEN：PASS_WITH_CHANGES** | 41 G1 tests passed；CSV/字典/4 SVG/manifest/哈希一致；A1 已回填 | 进入 G5 论文数字/图冻结 |
| G2 Q1/Q2 | 8/11 23:30 | 建模负责人 | **GREEN：PASS_WITH_LIMITATIONS** | 27 工况；GroupKFold 无电芯重叠；SOH 四模型；RUL 43 事件 + 65 右删失；A2 已同步 | 论文回指事实表与 A2 |
| G3 A3 筛选/编组 | 8/12 08:30 | 建模负责人 | **GREEN：PASS_WITH_ASSUMED_THRESHOLDS** | 85/108 过门槛；3167 候选组；三套 MILP；重复分配 0；A3 已同步 | 门槛/权重保持 UPDATEABLE |
| G4 多工况 | 8/12 13:30 | 建模负责人 | **GREEN：PASS_WITH_DOMAIN_AND_DESIGN_LIMIT** | 5 seed；5 工况；3 批次代理；7 参数 OAT；4 类 OOD 全弃权；场景内重新筛选/编组 | 论文不得称 Q3 同组电芯已纵向验证 |
| G5 数字与图冻结 | 8/12 18:00 | 总负责人 | **AMBER：A5 技术层 READY；论文逐项回指未完成** | `PAPER_TECHNICAL_BRIDGE.md`；9/9 gates；56 tests；正文数字/图号逐项回指 | 主笔只从 A5/事实表取数；停止新模型 |
| G6 独立红队/完整性 | 8/13 09:30 | 总负责人组织；两位队友复核 | **GRAY** | 引用、数据、泄漏、可复现、7 类 AI 研究失败模式清零或有书面处置 | 等完整草稿 |
| G7 内容冻结 | 8/13 14:00 | 论文主笔；总负责人验收 | **GRAY** | 摘要-正文-图表-结论一致；只剩格式或 P0 修复 | 等 G6 |
| G8 提交包 | 8/13 16:30 | 建模负责人打包；全员双检 | **GRAY** | PDF + 支撑材料 + README + 环境 + 哈希；两台设备打开检查 | 等 G7 |
| 首次上传 | 8/13 18:30 | 总负责人 | **GRAY** | 上传成功并下载回检 | 预留 90 分钟事故缓冲 |

说明：原设计中的 G2 `8/11 11:00` 早于现行 G1 `8/11 15:00`，已经不可执行。上表是恢复排期；G1 验收时再根据实测耗时压缩范围，不顺延 G5-G8。

## 4. G0-R 通过后的立即派工

### 总负责人（你）

1. 通知建模负责人从远端 `main` 的 G0-R clean line 开始 G1。
2. 19:30 收最小生成器、失败/通过测试和首份运行状态；没有哈希不算完成。
3. 论文主笔可立即写 G0 方法与变量段，但结果、精度和寿命数字仍等 A1 验收。

### 发给建模负责人

```text
【G0-R PASS / 立即执行】远端 `main` 的 G0-R clean line 已是唯一事实源，开始 G1。

总负责人已签署的有效路线仍是：NMC + CALCE INR18650-20R；NASA 只作外部/归一化对照；分段平方根容量/内阻退化；25--50 degC、0.5--2C、DOD 50/80/100、CC-CV；自设系数均为 ASSUMED。

请你本人指挥 Codex，先从干净 `main` 建立 G1，不引入复杂模型：
1. 先写并运行必失败测试，再实现分段平方根容量/内阻生成器。
2. 只使用 NMC/CALCE 边界；NASA 仍为 SECONDARY_ONLY，不合并拟合。
3. 至少生成 3 颗电芯的固定 seed 冒烟 CSV、数据字典和 4 类合理性图。
4. A1 必须记录运行命令、配置、seed、测试 stdout、输出文件和 SHA-256；不能预填结果。
5. 19:30 回报固定格式；代码负责人必须亲自复跑最终命令。

19:30 回报：
状态：DONE / DONE_WITH_CONCERNS / BLOCKED
分支/commit：
代码/配置/测试路径：
运行命令与测试结果：
输出文件与 SHA-256：
已知限制：
需要总负责人决定：
下一硬截止：8/10 21:30 固定种子冒烟输出
```

### 发给论文主笔

```text
【G0-R PASS / 18:00 前状态回报】开始准备论文方法段，仍不写结果。

请你本人维护腾讯文档，豆包只能做结构与措辞候选：
1. 回报腾讯文档链接，并确认三人权限。
2. 依据 NMC/CALCE G0 包完成方法、变量、假设和流程段；仿真数据仍称仿真。
3. 列出已完成章节、字数或段落数；不要只写“在写”。
4. 统计证据表条目数、已人工打开核验数、仍保留的 [[EVIDENCE REQUIRED:Ax-ID]] 数。
5. 不填预测精度、寿命、收益、安全阈值或模型优越性数字。

18:00 回报：
状态：DONE / BLOCKED
腾讯文档链接与权限：
已完成章节（附字数/段落数）：
证据表：总数 / 已人工核验 / 待核验：
保留的 EVIDENCE REQUIRED 数：
等待 A1-A4 的位置：
需要总负责人决定：
下一硬截止：8/11 10:30 G1 方法与变量段候选稿
```

## 5. G0-R 验收记录

以下六项必须全部有证据：

- [x] 所有生效文件的主线均为 NMC + CALCE INR18650-20R。
- [x] NASA 明确未进入当前实验、训练、验证或参数估计；未来若加入须独立建对照。
- [x] 生成公式、范围、seed、参数标签与已签署 G0 记录一致。
- [x] 两个 `.txt` 台账字段完整且引用路径存在；比赛期间不再迁移扩展名。
- [x] A1 尚未生成，当前没有虚构代码、测试、输出、哈希或结果。
- [x] 远端 `main` 的 G0-R clean line 可见，`git diff --check`、链接和台账自检通过。

结论模板：

```text
G0-R：PASS
验收 commit：G0-R clean line（父提交 `5076b05`；准确 hash 以远端 `git log` 为准）
负责人批准：2026-08-10 17:36 CST
未通过项：无
允许启动的下一任务：G1 最小生成器、测试、固定种子冒烟
禁止启动的任务：预测器、编组优化、复杂深度模型
下一检查点：19:30 G1 首次回报
```

## 6. 固定汇报协议

以后所有队友和 Agent 只按以下六行汇报；没有证据的 `DONE` 自动降为 `IN_PROGRESS`：

```text
状态：DONE / IN_PROGRESS / BLOCKED
Owner：
产物：链接或 commit + 文件路径
验证：命令、测试数、关键哈希或人工核验数
阻塞：问题 + 等谁决定；无则写 NONE
下一步：动作 + CST 截止
```

升级规则：

- `P0`：路线冲突、数据泄漏、引用/结果疑似虚构、核心结果不可复现。立即停工，10 分钟内报总负责人。
- `P1`：单个图表、脚本、章节或来源受阻。到最近检查点报告，优先降级范围。
- `P2`：措辞、样式、非核心图。记录后批量处理，不打断关键路径。

## 7. 总负责人当前决策队列

| 决策 | 默认动作 | 最晚决定 |
|---|---|---:|
| 是否改变 NMC + CALCE 主线 | **不改变**。已有签署记录；只有你明确推翻才重开 G0 | 已执行默认 |
| CALCE 原始数据若无法及时取得 | 保留透明假设生成器；删除“实测校准”措辞；NASA 保持未使用，不写已完成外部趋势检查 | 已执行 |
| G1 无法在 8/11 15:00 通过 | 降级到一个可解释方程 + 个体随机效应 + 完整测试，不加复杂模型 | 8/11 15:00 |
| 官方评分细则仍未提供 | 不阻塞；按可复现、解释性、闭环和论文一致性四条主标准推进 | 持续 |

## 8. 论文与支撑材料双轨状态

| 轨道 | Owner | 当前状态 | 允许输入 | 禁止输入 |
|---|---|---|---|---|
| GitHub 证据链 | 建模负责人 | **GREEN：G1-G4 技术证据已提交** | A1-A4 列出的固定配置、测试、CSV、字典、SVG、manifest 和哈希 | 模板脚本、聊天数字、未验收 Agent 输出 |
| A5 技术过渡层 | 总负责人 | **GREEN_WITH_LIMITATIONS：已与 `论文草稿.md` 逐节对齐；Stage 2.5 未通过** | 草稿映射、假设/模型/实验/结论、证据矩阵、更新注册表 | 将模板冲突句或候选引用当作已验证事实 |
| 腾讯文档正文 | 论文主笔 | **UNKNOWN：等待 18:00 状态回报** | 已验收 A0-A4 证据包、人工核验文献 | 未验收图表/数字、豆包编造引用 |
| 最终裁决 | 总负责人 | **ACTIVE** | commit、测试、哈希、证据表、人工复核 | “看起来可以”“Agent 说完成” |

下一次更新触发：收到论文主笔整合回报、完成 G6 红队复核，或任一 `P0` 事件。
